<EXTREMELY-IMPORTANT>
你在本项目的操作必须遵循 Harness Engineering 原则。
在执行任何代码编写之前，你必须**按顺序**完成以下 3 步检查。
如果不进行这 3 步，你是在违规操作。
</EXTREMELY-IMPORTANT>

## 1. 明确你的环境与护栏 (Read Guardrails)
你必须首先了解这个项目的规则和曾经踩过的坑。
- 🏛️ **架构规约**: 阅读 `ARCHITECTURE.md` (包含目录结构、依赖规则、技术栈限制)。
- 🩹 **血泪教训**: 阅读 `LEARNINGS.md` (包含本项目特有的环境配置坑、API 潜规则：**绝不要在同一个地方跌倒两次**)。

## 2. 确定你的起点 (Check State)
你是一个长时任务智能体。不要依赖跨越数百条记录的聊天历史来猜测当前的进度。
- 📊 **当前状态**: 阅读 `docs/agent-state/PROGRESS.json` (或 `ACTIVE_TASKS.md`)，找到当前标记为 `in_progress` 的任务。
- 📝 **设计文档**: 如果你的任务需要了解高层设计，去查阅 `docs/design-docs/`。
- 🏃 **执行计划**: 如果任务被拆解为了多个步骤，去查阅 `docs/exec-plans/` 获取详尽的 Checkbox 清单。

## 3. 遵从工作流指令 (Use Skills)
你不应该像一个初级程序员一样随心所欲地写代码。
- 🛠️ **加载技能**: 查看 `SKILLS/` 目录。如果你在进行具体任务（如：修复 Bug、编写测试、生成功能），你必须调用对应的技能文件（例如 `SKILLS/test-driven-development.md`）并严格遵循它的流程。

---

## 🛑 结束回合之前的强制检查清单 (Pre-completion Checklist)
在你告诉用户“任务已完成”之前，你必须自己确认：
1. [ ] **测试即验证**: 我是否运行了 `npm test` (或项目的测试命令) 并且全部通过？
2. [ ] **更新状态机**: 我是否更新了 `docs/agent-state/PROGRESS.json` 把该任务标记为了已完成？
3. [ ] **反哺知识库**: 我在这个过程中是否遇到了耗时超过 5 分钟的疑难杂症？如果是，我是否将解决方案补充进了 `LEARNINGS.md`？

<EXTREMELY-IMPORTANT>
如果你没有勾选上面的三个框，**你就不算完成了任务**。
立刻去执行验证和存档！
</EXTREMELY-IMPORTANT>
